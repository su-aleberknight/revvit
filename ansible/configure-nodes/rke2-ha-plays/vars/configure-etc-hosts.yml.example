---
# VIP managed by kube-vip — added to /etc/hosts on all nodes.
# rke2_vip_ip and rke2_vip_hostname are pulled from the [vip] inventory group.
# No variables need to be defined here — all values are derived from inventory.
