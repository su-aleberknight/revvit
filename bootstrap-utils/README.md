# Scripts

Utility scripts for managing the SUSECON lab environment.

install ansible (dnf/zypper install ansible)
create git secret
setup .ssh/config
create susecon-ssh
add nodes to /etc/hosts on bootstrap node (this is for the distributing ansible keys)
distribute ansible key to all nodes
git clone <repo>
done with scripts
